# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> 이 파일은 **새 세션마다 자동 로드**됩니다. 이 프로젝트에서 일할 때의 **규약(명료화·통제)·현재 상태·핵심 참조**를 한 페이지로 압축합니다.
> 상세 기획은 [PLAN.md](PLAN.md)를 단일 진실원천(SSOT)으로 따르세요.

## 한 줄 정의

**URL을 입력하면 해당 사이트를 자동 점검하여 ① 취약점 레포트 + ② 취약점별 조치 가이드 를 산출하는 합법·방어용 웹 취약점 분석 시스템 (SecurityGuardrails).**

## 현재 상태

- **Phase 1 MVP 구현됨 (Sprint 1).** Python 패키지 `sg/` — 승인/스코프 게이트 + **Passive 점검**(보안 헤더·TLS·쿠키·정보 노출) + 조치 가이드 포함 **JSON 레포트**. 스택: **Python (FastAPI)**. 백로그: [backlog/sprint-1.md](backlog/sprint-1.md).
- **미구현(추측 금지):** Active(능동) 점검, 프론트엔드/대시보드, DB·큐, ZAP/Nuclei 연동 → Sprint 2+. 위 "미구현"을 있는 것처럼 적지 말 것. 상위 정의 출처는 항상 `PLAN.md`.

## 핵심 명령어 (Python MVP)

```bash
pip install -e ".[dev]"                                   # 설치(개발 의존성 포함)
ruff check . && mypy sg && pytest -q                      # 린트·타입·테스트 (CI와 동일)
python -m sg scan https://app.internal --authorized-by 이름   # CLI 점검 (승인 필수)
pip install -r requirements-docs.txt && mkdocs serve      # 문서 사이트 로컬 미리보기

# 웹 UI (브라우저에서 도메인 입력 → 결과 표). 접근키 + 허용도메인 둘 다 필수(fail-closed):
$env:SG_API_KEY="키"; $env:SG_AUTHORIZED_DOMAINS="next-securities.com"
uvicorn sg.api:app --host 127.0.0.1 --port 8000           # http://127.0.0.1:8000
```

> ⚠️ **승인된 자산만**: 스캔은 `--authorized-by` 필수 + 대상 호스트가 스코프(allowed_hosts) 안에 있어야 한다. 미승인/만료/범위밖이면 `AuthorizationError`로 차단(점검 0건).

---

## 🔒 통제 규약 (Control / Governance) — 최우선·강제

> 이 도구는 이중 용도(dual-use)다. 아래 규칙은 어떤 기능·편의보다 **상위**이며, 위반 소지가 있으면 작업을 멈추고 확인한다. 출처: [PLAN.md](PLAN.md) 0장 「대원칙」.

1. **승인된 자산만 점검.** 어떤 스캔/네트워크 요청 코드도 실행 전에 **점검 승인(Scope Authorization)** 을 검증해야 한다. 소유·서면승인 없는 대상은 큐에 넣지 않는다.
2. **스코프 격리.** 크롤러·페이로드가 승인된 도메인/경로 밖으로 새지 않게 한다(out-of-scope 링크 follow 금지).
3. **비파괴 기본값.** 기본 모드는 읽기/탐지. 데이터 변경·삭제·대량요청 등 파괴적 동작은 **기본 비활성** + 명시적 동의 플래그에서만.
4. **DoS 금지·속도 제한.** 동시요청/RPS 상한·백오프 필수. 부하·서비스 마비 유발 코드는 작성하지 않는다.
5. **감사 로그 불변 기록.** 누가·언제·무엇을·어떤 강도로 점검했는지 추적 가능해야 한다.
6. **데이터 최소수집·보호.** 수집 증거(요청/응답)는 민감정보 마스킹, 보존기간 후 파기.
7. **악용 가능 산출물 주의.** 조치 가이드는 *방어/수정* 관점으로 쓴다(무기화된 익스플로잇 제공 금지). 증거 PoC는 재현 검증 목적에 한정.

> 코드 리뷰/생성 시 위 7개는 **회귀 테스트 대상**이다. 안전장치를 우회·약화하는 변경은 거부하거나 사용자에게 명시적으로 확인한다.

---

## 🧭 명료화 규약 (Clarification Protocol)

> 추측해서 진행하다 잘못된 산출물을 만드는 것보다, **짧게 묻고 정확히 하는 것**이 이 프로젝트의 합의된 방식이다. (실제로 PLAN 수립 시에도 대상·목적을 먼저 확인하고 진행했다.)

**작업 전에 반드시 확인할 것:**

- **대상·스코프** — 어떤 URL/도메인을, 어떤 강도(Passive/Safe-Active/Full)로? 승인은 있는가?
- **파일 위치** — 산출물을 어느 경로에 만들/고칠지 모호하면 묻는다(이 저장소는 빈 디렉터리로 시작했음).
- **성공 기준** — "끝"의 정의를 한 줄로 복창한 뒤 큰 작업을 시작한다.

**규칙:**

1. 요구가 모호하면 **1~3개의 초점화된 질문**으로 좁힌 뒤 진행한다(끝없는 질문 금지 — 합리적 기본값이 있으면 그것을 밝히고 진행).
2. **가정은 드러내서 적는다.** "X라고 가정하고 진행한다"를 먼저 명시.
3. **되돌리기 어렵거나 외부로 나가는 작업**(파일 삭제/덮어쓰기, 네트워크 점검 실행, 커밋/푸시, 외부 전송)은 **사전 확인** 후 수행한다. 한 곳의 승인이 다른 곳으로 확장되지 않는다.
4. 결과는 **사실대로** 보고한다 — 테스트 실패는 출력과 함께, 건너뛴 단계는 건너뛰었다고, 검증된 것만 "완료"라고 말한다.
5. **표준 우선.** 취약점 분류·심각도·조치는 OWASP Top 10 · OWASP WSTG · CWE · CVSS에 매핑한다(임의 명명 금지).

---

## 문서 작성 규약 (Docs Convention)

- 기획·역할·절차 문서는 **한국어 PRD 스타일**(표·다이어그램 중심)로 작성한다 — `PLAN.md` 형식을 따른다.
- 모든 계획 변경은 `PLAN.md`에 반영하고, 섹션 번호 일관성을 유지한다.
- "계획(proposed)"과 "구현됨(actual)"을 항상 구분해 표기한다.

## 핵심 참조 (Key References)

| 찾는 것 | 위치 |
|---|---|
| 마스터 기획서(SSOT) | [PLAN.md](PLAN.md) |
| 역할별 상세 JD · 인덱스 | [harness/README.md](harness/README.md) |
| 운영 철학(프로세스 기반 자유도) | harness/README.md §운영 철학 |
| 역할↔통제 경계(자유도 vs 통제) | 각 `harness/*.md` §2 |
| 합법·승인 대원칙 | PLAN.md §0 |
| 기능 요건 | PLAN.md §2 |
| 취약점 분석 절차(S0~S7) | PLAN.md §3 |
| 역할 구성 | PLAN.md §4 |
| 애자일 프로세스 | PLAN.md §5 |
| 역할 오케스트레이션(RACI·핸드오프·에스컬레이션) | PLAN.md §6 |
| 기술 스택(제안) | PLAN.md §7 |
| 개발 로드맵(Phase/Sprint) | PLAN.md §8 |
| 점검항목 ↔ OWASP/CWE 매핑 | PLAN.md 부록 A |
| 검증용 취약 테스트 앱 | PLAN.md 부록 C (Juice Shop·DVWA·WebGoat — 격리 환경 전용) |

## 기술 스택 (제안 — 미구현)

| 레이어 | 제안 | 비고 |
|---|---|---|
| 엔진/백엔드 | Node.js(TS) 또는 Python | 검증된 OSS(ZAP/Nuclei) 래핑 권장 |
| 프론트엔드 | React + TypeScript | 대시보드·레포트 뷰 |
| DB | PostgreSQL | 스캔·자산·취약점·감사로그 |
| 큐/워커 | Redis + 격리 워커 | 동시성·속도제한·격리 |
| 리포트 | HTML/PDF + JSON/SARIF | CI 연동 |

> ⚠️ 자체 페이로드를 처음부터 만들기보다 **검증된 엔진을 래핑**하고 그 위에 *승인·스코프·리포트·조치가이드* 레이어를 얹는 것이 MVP 최단 경로.

## 자주 만날 함정 (현재 알려진 것)

- **빈 저장소 가정 금지** — 실행 전 실제 파일 존재를 확인한다(`Glob`/`Read`). 과거 워크스페이스에 다른 프로젝트(InterviewBot)가 섞여 있었음.
- **명령어 환각 금지** — 실제 명령은 위 "핵심 명령어"에 한정. 미구현 기능의 명령을 지어내지 말 것.
- **테스트는 격리 환경에서만** — 취약 테스트 앱은 외부에 노출하지 말고 로컬/격리 네트워크에서만 구동.
