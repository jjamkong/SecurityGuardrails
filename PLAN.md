# 웹 취약점 분석 시스템 (SecurityGuardrails) — 프로젝트 마스터 기획서 (PRD)

> **버전:** MVP 1.0 (초안)
> **문서 목적:** 프로젝트 리더(PL)가 팀을 즉시 킥오프하고, 역할·절차·애자일 프로세스를 한 문서에서 합의할 수 있도록 구조화한 마스터 기획서
> **한 줄 정의:** **URL을 입력하면 해당 사이트를 자동 점검하여, ① 취약점 레포트 와 ② 취약점별 조치 가이드 를 산출하는 합법·방어용 웹 취약점 분석 시스템**

---

## 0. 대원칙 — 합법·승인 기반 점검 (Non-Negotiable)

> 취약점 스캐너는 **이중 용도(dual-use)** 도구다. 잘못 쓰면 무단 침해가 된다. 아래는 기능보다 **상위**의 강제 규칙이며, 모든 Phase에서 우선한다.

| 원칙 | 구현 방식 |
|------|-----------|
| **소유·승인된 자산만 점검** | 스캔 실행 전 **대상 소유권/서면 점검 승인(Scope Authorization)** 등록을 강제. 미승인 도메인은 큐에 들어가지 않음 |
| **스코프 화이트리스트** | 승인된 도메인/IP/경로 범위 밖으로 크롤러·페이로드가 새어나가지 않도록 차단 (out-of-scope 링크 follow 금지) |
| **비파괴(Non-destructive) 기본값** | 기본 모드는 **읽기/탐지 위주**. DELETE/DROP·대량요청·서비스 마비를 유발하는 페이로드는 기본 비활성, 명시적 동의 시에만 |
| **DoS 금지 · 속도 제한** | 동시 요청 수·RPS 상한, 백오프. 부하 테스트는 본 도구의 목적이 아님 |
| **전 과정 감사 로그** | 누가·언제·어떤 대상을·어떤 강도로 스캔했는지 불변 로그로 기록 (책임 추적성) |
| **데이터 최소수집·보호** | 수집한 증거(요청/응답)는 민감정보 마스킹, 보존기간 후 자동 파기 |

---

## 1. 프로그램 개요 (Project Overview)

| 항목 | 내용 |
|------|------|
| **시스템 명칭** | SecurityGuardrails — 웹 취약점 분석 시스템 (MVP) |
| **개발 목적** | 보안 담당자/개발자가 자사 웹 서비스의 URL만 입력하면, 외부 유료 스캐너 의존 없이 사내에서 **취약점 탐지 → 위험도 평가 → 조치 가이드** 까지 한 번에 받도록 하는 자체 점검 플랫폼 |
| **핵심 컨셉** | 승인 기반 점검 · 수동(passive)+능동(active) 2단 분석 · **조치 가이드까지 포함한 실행 가능한 레포트** |
| **준거 표준** | OWASP Top 10, OWASP WSTG(Web Security Testing Guide), CWE, CVSS v3.1/v4.0 |

### 산출물(필수사항) 정의

1. **취약점 레포트** — 발견 항목 목록, 심각도(CVSS), 위치(URL/파라미터), 증거(PoC 요청·응답), 표준 매핑(OWASP/CWE)
2. **취약점별 조치 가이드** — 각 항목마다 *무엇이 / 왜 위험한가 / 어떻게 고치는가(코드·설정 예시) / 검증 방법*

---

## 2. 핵심 기능 요건 정의 (Functional Requirements)

### 🎯 A. 스캔 입력 · 스코프 관리

- URL/도메인 입력, **점검 승인(Scope Authorization)** 등록·검증(소유권 확인: DNS TXT 또는 파일 업로드 토큰).
- 스캔 강도 프로파일 선택: `Passive`(무해) / `Safe-Active`(저위험 능동) / `Full`(명시 동의 필요).
- 인증 필요 영역 점검을 위한 로그인 세션/토큰 주입(선택).

### 🔎 B. 분석 엔진 (Scan Engine)

- **자산 식별/크롤링:** 사이트맵·robots·링크 따라 엔드포인트/폼/파라미터 수집 (스코프 내 한정).
- **기술 핑거프린팅:** 서버·프레임워크·라이브러리·버전 식별 → 알려진 취약점(CVE) 매칭.
- **수동(Passive) 점검:** 보안 헤더(CSP·HSTS·X-Frame-Options 등), TLS/인증서, 쿠키 플래그, 정보 노출(.git/.env/백업파일/디렉터리 리스팅/에러 스택), CORS 설정.
- **능동(Active) 점검(승인 시):** OWASP Top 10 — 인젝션(SQLi/명령), XSS, 경로 탐색, 인증·세션 결함, 접근통제(IDOR), CSRF, SSRF, 오픈 리다이렉트, 보안 설정 오류 등.
- **검증/오탐 제거:** 탐지 결과 재현·확인하여 false positive 최소화(증거 자동 캡처).

### 📊 C. 리포트 + 조치 가이드 (필수)

- 항목별: 심각도(CVSS 점수·벡터), 영향, 위치, 재현 절차, 증거.
- **조치 가이드 지식베이스(KB)** 연결: 취약점 유형(CWE)별 표준 수정 가이드 + 스택별 코드/설정 예시.
- 출력 포맷: 웹 대시보드 + 내보내기(PDF/HTML/JSON/SARIF) + 경영진 요약(Executive Summary).
- 재스캔 시 **이전 대비 변화(신규/해결/잔존)** 추적.

### 🖥️ D. 대시보드 / 운영

- 스캔 진행 현황, 자산별 취약점 추이, 미해결 항목 SLA, 사용자/권한 관리, 감사 로그 열람.

---

## 3. 취약점 분석 절차 (Work Procedure) — 표준 점검 파이프라인

> "작업을 위한 절차". 모든 스캔은 아래 8단계를 순서대로 거치며, 각 단계는 다음 단계의 입력이 된다.

| 단계 | 절차 | 산출(입력→출력) | 책임 역할 |
|------|------|------------------|-----------|
| **S0. 승인·스코프 확정** | 대상 소유권/점검 승인 검증, 스코프(도메인·경로·강도) 확정 | 승인 레코드 + 스코프 정의 | PM · 컴플라이언스 |
| **S1. 정보 수집(Recon)** | 크롤링·자산/엔드포인트 식별·기술 핑거프린팅 | 자산 인벤토리, 공격면 맵 | 백엔드 · 보안연구 |
| **S2. 수동 점검(Passive)** | 헤더·TLS·쿠키·정보노출 등 무해 점검 | 1차 후보 결함 | 보안연구 · 백엔드 |
| **S3. 능동 점검(Active)** | 승인 강도에 맞춰 OWASP Top10 페이로드 테스트 | 2차 후보 결함 | 보안연구(Pentester) |
| **S4. 검증·오탐 제거** | 후보 결함 재현·확인, 증거 캡처, 중복 병합 | 확정 취약점 + 증거 | 보안연구 · QA |
| **S5. 위험도 평가** | CVSS 산정, 비즈니스 영향 반영 우선순위화 | 우선순위 리스트 | 보안연구 · PM |
| **S6. 레포트·조치 가이드 생성** | 항목별 설명 + CWE 매핑 + 수정 가이드(코드/설정) | **최종 레포트(필수 산출물)** | 리포트 엔지니어 |
| **S7. 재검증(Re-test)** | 조치 후 재스캔, 해결 여부 확인·이력화 | 변화 추적 보고 | QA · 보안연구 |

---

## 4. 필요 인력 구성 및 요건 (Team Composition)

| 역할 | 주요 담당 업무 | 핵심 필요 역량 |
|------|---------------|----------------|
| **PM / 기획 (Product Owner)** | • 요구사항·스코프 정책 정의<br>• 백로그 우선순위·일정·리스크 관리<br>• 승인/컴플라이언스 프로세스 설계 | • 보안 점검 도메인 이해<br>• 애자일 백로그 운영 |
| **보안 아키텍트** | • 스캐너 엔진 아키텍처·룰셋 체계 설계<br>• 안전장치(스코프 격리·속도제한·비파괴) 설계 | • 웹 보안 아키텍처<br>• 스캐너/룰엔진 설계 경험 |
| **취약점 연구원 / Pentester** | • 탐지 로직(OWASP Top10) 정의<br>• 오탐 검증·증거 수집<br>• 조치 가이드 기술 검수 | • OWASP WSTG 실무<br>• 웹 익스플로잇·취약점 분석 |
| **백엔드 개발자** | • 스캔 잡 오케스트레이션·워커·큐<br>• 크롤러·핑거프린팅·API 구현 | • 비동기 작업/큐 처리<br>• HTTP 스택·동시성 제어 |
| **프론트엔드 개발자** | • 대시보드·스캔 현황·레포트 뷰<br>• 결과 필터/드릴다운 UI | • 데이터 시각화<br>• 상태관리·표/차트 UI |
| **리포트·KB 엔지니어** | • CVSS 산정 로직·리포트 렌더링<br>• CWE별 조치 가이드 지식베이스 구축 | • 보안 표준(CVSS/CWE) 이해<br>• 문서/리포트 자동화 |
| **DevOps / 인프라** | • 스캔 워커 격리·샌드박스·스케일<br>• 시크릿/감사로그/모니터링 | • 컨테이너·격리 네트워크<br>• 관측성(로깅/메트릭) |
| **QA / 검증** | • 취약 테스트 앱 대상 탐지율·오탐율 측정<br>• 회귀·안전장치 검증 | • 보안 테스트 시나리오<br>• 측정 기반 품질관리 |
| **법무 / 컴플라이언스 (겸임 가능)** | • 점검 승인·계약·데이터 보존 정책 검토 | • 정보보호 법규/계약 이해 |

> 소규모 팀이면 **PM · 보안연구 · 풀스택 · QA** 4인으로 시작하고, 보안아키텍트/DevOps/리포트엔지니어/컴플라이언스는 겸임 후 분리.

---

## 5. 애자일 프로세스 (Agile Process)

> "애자일하게 결론에 도달" = **2주 스프린트마다, 의도적으로 취약하게 만든 테스트 앱(DVWA/OWASP Juice Shop/WebGoat)에 대해 탐지율·오탐율을 측정**하여 스캐너의 신뢰도를 수치로 끌어올린다. 이것이 결론(신뢰 가능한 스캐너)에 수렴하는 피드백 루프다.

### 5.1 운영 방식

- **스프린트:** 2주 고정. 각 스프린트 끝에 **데모 가능한 증분(working increment)** 산출.
- **세레모니:** 스프린트 계획 → 데일리 스탠드업(15분) → 스프린트 리뷰(이해관계자 데모) → 회고(개선 1~2건 즉시 반영).
- **백로그:** 제품 백로그(Epic→Story) / 스프린트 백로그 / 번다운으로 가시화.

### 5.2 측정 기반 수렴 루프 (핵심)

```
탐지 룰 추가/수정 ─▶ 취약 테스트 앱에 스캔 실행 ─▶ 탐지율·오탐율 측정
        ▲                                                   │
        └──────────── 회고에서 룰 보정 우선순위 결정 ◀────────┘
```

- 매 스프린트 KPI: **탐지율(Recall)↑, 오탐율(False Positive)↓, 평균 스캔시간**.
- "결론"의 정의 = 합의한 임계치(예: Top10 핵심 항목 탐지율 ≥ 목표, 오탐율 ≤ 목표) 도달.

### 5.3 Definition of Ready / Done

- **DoR:** 스토리에 대상 취약점 유형·예상 탐지 시그니처·테스트 케이스(취약/정상 모두)·승인 스코프가 정의됨.
- **DoD:** 코드+테스트 통과 · 테스트 앱에서 탐지 확인 · **오탐 케이스 1건 이상 통과(정상 사이트 오탐 없음)** · 레포트에 조치 가이드 포함 · 문서 갱신 · 안전장치(스코프/속도제한) 회귀 통과.

---

## 6. 역할 오케스트레이션 (Role Orchestration)

> 3장(절차)·4장(역할)·5장(애자일)을 하나로 묶는 **협업 계약**. 핵심 질문 = *"누가 무엇을 만들어, 누구에게 넘기고, 막히면 누가 결정하는가."*

### 6.1 두 겹의 오케스트레이션

오케스트레이션은 두 층위에서 동시에 돈다. 역할은 두 층을 잇는 매듭이다 — **개발 시점엔 사람이 산출물을 릴레이하고, 실행 시점엔 시스템이 그 사람이 만든 모듈을 자동 호출한다.**

| 층위 | 무엇을 조율하나 | 주체 | 비고 |
|------|----------------|------|------|
| **(A) 팀 오케스트레이션** | 개발 중 역할 간 산출물 흐름 | 사람(PM이 지휘) | 스프린트 백로그 = 악보 |
| **(B) 런타임 오케스트레이션** | 스캔 실행 시 모듈 호출 순서 | 시스템(Job Orchestrator) | 각 역할이 특정 파이프라인 모듈을 *소유* |

```
[B] 런타임 파이프라인 (스캔 1회):
  Orchestrator ─▶ S0 승인검증 ─▶ S1 크롤러 ─▶ S2 Passive ─▶ S3 Active ─▶ S4 검증 ─▶ S5 CVSS ─▶ S6 리포트 ─▶ S7 재검증
                    (CMP/PM)      (BE)        (SEC/BE)      (SEC)        (SEC/QA)   (SEC)     (RPT)        (QA)
                       └────────── 각 단계의 "소유 역할"이 [A] 개발 시점에 그 모듈을 만든다 ──────────┘
```

### 6.2 RACI 매트릭스 (절차 S0~S7 × 역할)

**R**esponsible(실행) · **A**ccountable(최종책임, 행당 1명) · **C**onsulted(자문) · **I**nformed(공유).
약어: PM·ARC(보안아키텍트)·SEC(보안연구/Pentester)·BE·FE·RPT(리포트·KB)·OPS(DevOps)·QA·CMP(컴플라이언스)

| 절차 | PM | ARC | SEC | BE | FE | RPT | OPS | QA | CMP |
|------|----|-----|-----|----|----|-----|-----|----|-----|
| S0 승인·스코프 | **A** | C | I | I | – | – | I | – | **R** |
| S1 정보수집 | I | **A** | C | **R** | – | – | C | – | I |
| S2 수동점검 | I | **A** | R | R | – | – | C | I | – |
| S3 능동점검 | I | A | **R** | C | – | – | C | I | I |
| S4 검증·오탐제거 | I | C | **A/R** | C | – | – | – | R | – |
| S5 위험도평가 | **A** | C | R | – | – | C | – | – | I |
| S6 레포트·조치가이드 | A | C | C | – | I | **R** | – | I | I |
| S7 재검증 | **A** | – | R | C | – | C | C | R | – |

> 읽는 법: S4(오탐 제거)의 **최종판정·실행은 SEC**, S6(레포트)의 **실행은 RPT·책임은 PM**, S0(승인)의 **실행은 CMP·책임은 PM**.

### 6.3 핸드오프 체인 — 산출물 계약 (Artifact Contracts)

각 단계는 **정해진 형식의 산출물**을 다음 역할에 넘긴다. 형식이 계약이므로, 필수 필드가 비면 다음 단계는 시작하지 않는다(= 자동 게이트).

| 핸드오프 | 산출물 | 필수 필드(계약) | 생산→소비 |
|----------|--------|------------------|-----------|
| S0→S1 | 스코프 정의 | 대상·허용경로·강도·유효기간·승인ID | CMP/PM → BE |
| S1→S2/3 | 자산 인벤토리 | URL·엔드포인트·파라미터·기술핑거 | BE → SEC |
| S3→S4 | 후보 결함 | 유형·위치·페이로드·원시응답 | SEC → SEC/QA |
| S4→S5 | 확정 취약점 | 재현절차·증거·중복병합키 | SEC/QA → SEC |
| S5→S6 | 우선순위 리스트 | CVSS벡터·점수·비즈니스영향 | SEC → RPT |
| S6→S7 | 최종 레포트 | 항목+CWE+**조치가이드(코드/설정)** | RPT → QA |

### 6.4 의사결정 · 에스컬레이션 경로

**결정권(누구 말이 최종인가):**

| 사안 | 최종 결정 | 근거 |
|------|-----------|------|
| 점검 합법성·스코프 | **CMP (veto = 스캔 중단권)** | 무단 점검 방지 (0장 대원칙) |
| 오탐 여부 | **SEC** (재현 데모로 판정) | 증거 기반 |
| 심각도·우선순위 | **PM** (SEC 입력 반영) | 비즈니스 영향 |
| 룰셋·아키텍처 표준 | **ARC** | 일관성·안전장치 |
| 릴리스(DoD 충족) | **QA + PM 합의** | 품질 게이트 |

**에스컬레이션:** 블로커 발생 → 데일리 스탠드업에서 PM이 1차 조정 → 24h 내 미해결 시 사안별 결정권자(ARC/CMP)로 상신 → 그래도 충돌 시 SEC가 **취약 테스트 앱 재현 데모**로 사실 판정. *오탐 논쟁은 토론이 아니라 재현으로 끝낸다.*

### 6.5 동기화 지점 (Sync Points)

| 종류 | 채널 | 참석/대상 | 목적 |
|------|------|-----------|------|
| 동기 | 스프린트 계획 | 전원 | 백로그→스프린트 배분 |
| 동기 | 데일리 스탠드업(15') | 전원 | 블로커·핸드오프 상태 |
| 동기 | 스프린트 리뷰(데모) | 전원+이해관계자 | 증분 시연·피드백 |
| 동기 | 회고 | 전원 | 룰 보정·프로세스 개선 1~2건 |
| 비동기 | PR 리뷰 | ARC/SEC가 룰·엔진 검수 | 코드 게이트 |
| 비동기 | 측정 대시보드 | 전원 | 탐지율·오탐율 실시간 공유 |
| 게이트 | Phase 전환 | PM 승인 | **안전장치 회귀 통과 필수** |

### 6.6 워크스루 — 취약점 하나의 여정 (오케스트레이션 예시)
>
> 한 건의 SQLi가 탐지→레포트까지 역할 사이를 어떻게 이동하는가.

```
사용자: URL 입력
  └─▶ S0  CMP/PM      승인·스코프 검증 통과 (미승인이면 여기서 차단)
      └─▶ S1  BE      크롤러가 /login 의 user 파라미터 발견 → 자산 인벤토리
          └─▶ S3  SEC 능동점검이 user 파라미터에서 SQLi 징후 탐지 → 증거(요청/응답) 캡처
              └─▶ S4 SEC+QA  재현 검증 → 확정 (QA가 정상 사이트 오탐 케이스도 교차 확인)
                  └─▶ S5 SEC  CVSS 8.6 산정 → PM이 "로그인 우회 가능" 비즈니스 영향 반영해 Critical 확정
                      └─▶ S6 RPT  CWE-89 조치가이드 KB 결합(파라미터 바인딩 코드 예시) → 레포트 항목 완성
                          └─▶ 개발팀 조치 후
                              └─▶ S7 QA+SEC  재스캔 → 해결 확인 → 이력화(해결됨)
```

---

## 7. 기술 스택 (제안)

| 구분 | 기술(제안) | 비고 |
|------|-----------|------|
| **Backend / 엔진** | Node.js(TypeScript) 또는 Python | 잡 큐·HTTP 제어, 오픈소스 스캐너(ZAP/Nuclei) 연동 용이 |
| **분석 보강** | OWASP ZAP API · Nuclei 템플릿 · 핑거프린트(Wappalyzer 류) | 바퀴 재발명 최소화 — 검증된 엔진 래핑 |
| **Frontend** | React + TypeScript | 대시보드·레포트 뷰 |
| **Database** | PostgreSQL | 스캔/자산/취약점/감사로그 |
| **Queue / Worker** | Redis + 워커(격리 컨테이너) | 동시 스캔·속도제한·격리 |
| **Report** | HTML/PDF + JSON/**SARIF** | CI 연동·도구 상호운용 |

> ⚠️ 자체 페이로드를 처음부터 만들기보다 **검증된 오픈소스 엔진(ZAP/Nuclei)을 래핑**하고, 그 위에 *승인·스코프·리포트·조치가이드* 레이어를 얹는 것이 MVP 최단 경로.

---

## 8. 개발 로드맵 (Phases / Sprints)

> PoC로 위험을 먼저 제거하고 비즈니스 가치를 붙여나가는 단계적 진행.

### Phase 1 — 골격 + 승인/스코프 + Passive PoC · 스프린트 1

URL 입력 → 승인/스코프 등록 → **무해한 수동 점검(헤더·TLS·쿠키·정보노출)** → 최소 레포트(JSON) 출력까지 **엔드투엔드 한 줄기**를 먼저 관통. 안전장치(스코프 격리·속도제한) 동시 적용.

### Phase 2 — 능동 점검 엔진 + 검증 루프 · 스프린트 2~3

ZAP/Nuclei 래핑으로 OWASP Top10 능동 점검 도입. **취약 테스트 앱 기반 탐지율/오탐율 측정 루프** 가동. 오탐 제거·증거 캡처 파이프라인 구축.

### Phase 3 — 레포트 + 조치 가이드(KB) + 대시보드 · 스프린트 4~5

CVSS 산정, CWE별 **조치 가이드 지식베이스** 연결, 웹 대시보드/내보내기(PDF/HTML/SARIF), 재스캔 변화 추적. → **필수 산출물(레포트+조치가이드) 완성.**

### Phase 4 — 강화·운영화 · 스프린트 6

인증 영역 점검, 권한/감사로그, 스캔 격리·스케일, 성능 튜닝, 회귀·안전장치 전수 검증 후 사내 배포.

| Phase | 스프린트 | 산출물 |
|-------|----------|--------|
| 1 | S1 | 승인/스코프 + Passive 점검 + 최소 레포트 (E2E PoC) |
| 2 | S2~S3 | 능동 점검 엔진 + 탐지율/오탐율 측정 루프 |
| 3 | S4~S5 | 레포트 + 조치 가이드 KB + 대시보드 (**필수 산출물 완성**) |
| 4 | S6 | 인증점검·감사·격리·성능 → 사내 배포 |

---

## 부록 A. 점검 항목 ↔ 표준 매핑 (초기 룰셋 시드)

| 분류 | 점검 항목(예) | OWASP/CWE |
|------|---------------|-----------|
| 설정/헤더 | CSP·HSTS·X-Frame-Options·쿠키 Secure/HttpOnly/SameSite 누락 | A05 보안설정오류 / CWE-693, CWE-614 |
| 전송보안 | 약한 TLS·만료/자가서명 인증서·평문 HTTP | A02 암호화 실패 / CWE-326 |
| 정보노출 | `.git`/`.env`/백업파일·디렉터리 리스팅·상세 에러 | A01/A05 / CWE-200, CWE-538 |
| 인젝션 | SQLi·명령 인젝션 | A03 인젝션 / CWE-89, CWE-78 |
| XSS | 반사형/저장형/DOM | A03 / CWE-79 |
| 접근통제 | IDOR·강제 브라우징 | A01 접근통제 / CWE-639, CWE-22 |
| 요청위조 | CSRF·SSRF·오픈 리다이렉트 | A01/A10 / CWE-352, CWE-918, CWE-601 |
| 구성요소 | 알려진 취약 버전 라이브러리(CVE) | A06 취약/구식 구성요소 |

## 부록 B. 데이터 모델 개요(초안)

```
ScopeAuthorization (대상·소유검증·강도·유효기간)
  └─ 1:N ─ Scan (시작시각·프로파일·상태)
              ├─ 1:N ─ Asset (URL·엔드포인트·기술핑거)
              └─ 1:N ─ Finding (유형·CWE·CVSS·증거·상태)
                          └─ 1:1 ─ RemediationGuide (KB 참조·코드/설정 예시)
AuditLog (행위자·대상·시각·강도)   // 불변
```

## 부록 C. 검증용 의도적 취약 앱 (QA 대상)

- **OWASP Juice Shop**, **DVWA**, **WebGoat** — 로컬/격리 환경에서만 구동, 탐지율·오탐율 측정 기준선.

---

> **다음 단계 제안:** 이 PLAN 확정 후 → ① 역할별 상세 JD(`harness/`) ② 초기 백로그(Epic/Story) ③ Sprint 1 범위 확정 순으로 진행. 원하시면 바로 이어서 만들어 드리겠습니다.
